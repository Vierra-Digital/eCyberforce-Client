Have every custom component of order history and downloads have modal inside of them so it can read the data easily without needing store

this can work, but for every modal make new state, or that or close == 0 default, onclick is == to index and only modal with same index opens